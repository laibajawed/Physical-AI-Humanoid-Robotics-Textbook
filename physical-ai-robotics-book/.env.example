# ChatKit Frontend Environment Configuration
# Copy this file to .env.local and fill in your values
#
# IMPORTANT: Docusaurus does NOT use NEXT_PUBLIC_* variables (that's Next.js)
# Use docusaurus.config.ts customFields instead

# =============================================================================
# Backend API URL
# =============================================================================

# Backend API URL (accessed via customFields in docusaurus.config.ts)
# Local development: http://localhost:8000
# Production: Your deployed backend URL (e.g., Railway, Vercel, etc.)
BACKEND_URL=http://localhost:8000

# =============================================================================
# Backend API Endpoints (for reference)
# =============================================================================
# POST /chat - Single turn Q&A (non-streaming)
# POST /chat/stream - Streaming responses (SSE)
# GET /history/{session_id} - Conversation history
# GET /health - Health check

# =============================================================================
# Algolia Search (optional)
# =============================================================================
# ALGOLIA_APP_ID=your-algolia-app-id
# ALGOLIA_API_KEY=your-algolia-api-key
# ALGOLIA_INDEX_NAME=your-algolia-index-name
# =============================================================================
# Better Auth Configuration
# =============================================================================

# Secret key for signing sessions (32+ characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=XxYGxA5nbzghVkgWzEQ2hEHd5nAloePM

# Better Auth URL (must match the deployed URL)
BETTER_AUTH_URL=http://localhost:3001

# Database URL (for Better Auth tables - user, session, account)
# Use the same Neon Postgres database as the backend
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require
