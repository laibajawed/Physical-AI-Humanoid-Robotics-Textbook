# RAG Agent Backend Environment Configuration
# Copy this file to .env and fill in your values
#
# IMPORTANT: Never commit .env with actual secrets to version control

# =============================================================================
# Required - API Keys
# =============================================================================

# Cohere API Key (for embeddings)
# Get from: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=your-cohere-api-key-here

# Gemini API Key (for LLM)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# =============================================================================
# Required - Vector Database (Qdrant Cloud)
# =============================================================================

# Qdrant Cloud URL
# Get from: https://cloud.qdrant.io/
QDRANT_URL=https://your-cluster.us-east4-0.gcp.cloud.qdrant.io:6333
QDRANT_API_KEY=your-qdrant-api-key-here

# =============================================================================
# Required - Session Database (Neon Postgres)
# =============================================================================

# Neon Postgres connection string
# Get from: https://console.neon.tech/
# Must include sslmode=require for Neon
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# =============================================================================
# Required - Frontend Configuration
# =============================================================================

# Docusaurus site base URL (for source citations)
DOCUSAURUS_BASE_URL=https://your-docusaurus-site.vercel.app/

# =============================================================================
# CORS Configuration (Required for frontend integration)
# =============================================================================

# Allowed CORS origins (comma-separated)
# Must include your Docusaurus frontend URLs
#
# For development:
#   http://localhost:3000 - Docusaurus dev server
#   http://localhost:5173 - Vite dev server (if used)
#
# For production:
#   Add your deployed frontend URL(s)
#
# IMPORTANT: Never use wildcard (*) in production per spec FR-028
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend.vercel.app

# =============================================================================
# Authentication (Better Auth JWT Validation)
# =============================================================================

# Better Auth URL for JWKS endpoint
# This is where the frontend Better Auth server is running
# Used to fetch public keys for JWT validation
BETTER_AUTH_URL=http://localhost:3001

# =============================================================================
# Optional - Server Configuration
# =============================================================================

# Server host and port
PORT=8000
HOST=0.0.0.0

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
